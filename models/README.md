# Models

Trained model files and exported classifiers should be saved here during development.

Large model artifacts are ignored by git by default. When a model is ready for sharing, document:

- Training data version
- Target labels
- Evaluation metrics
- Alert threshold used for the live demo
